# מרכזייה עצמית ל-ha_ivr — התקנה עם הסקריפט

מדריך זה מקים מרכזיית Asterisk המחוברת לאינטגרציית **ha_ivr** של Home
Assistant: תפריט טלפוני, עוזר קולי בשיחה חופשית, והתראות קוליות יזומות.
כל ההתקנה אוטומטית; מה שנשאר ידני הוא רק מה שחוצה בין השרת ל-HA (טוקן,
סוד, פרטי ספק) — כי אלה סודות שגרים בשני צדדים.

> **חלוקת האחריות:** כל הבחירות (תפריט, נמענים, טקסטים) חיות ב-ha_ivr,
> בממשק הגרפי בעברית. הטרמינל עושה רק את מה שהוא חייב: לבנות, ולהחזיק
> סודות מקומיים. הטרמינל באנגלית בכוונה — עברית לא מרונדרת אמין
> בטרמינלים (ראו הערה בסוף).

> **שני מסלולים לעוזר הקולי** (המתקין מגדיר את שניהם):
> **1. AudioSocket** — המסלול העיקרי, PCM גולמי ל-ha_ivr, בלי קודק.
> זה מה שהמדריך הזה מגדיר.
> **2. SIP → העוזר המובנה של HA** — מסלול גיבוי דרך אינטגרציית `voip`
> של HA (שלוחת `ha_endpoint` ב-Opus). מוגדר אוטומטית אך בשימוש רק אם
> מכוונים טראנק להקשר `from-trunk`; דורש שאינטגרציית voip תותקן ב-HA.
> רוב ההתקנות משתמשות ב-AudioSocket בלבד.

---

## דרישות מוקדמות

- **Debian 13 (trixie)** נקי, גישת root.
- **Home Assistant** עם האינטגרציה **ha_ivr** מותקנת, ורשומת ספק
  **"מרכזייה עצמית"** שנוצרה (הגדרות → הוסף אינטגרציה → IVR).
- **טראנק SIP** מספק טלפוניה (למשל ימות) — פרטי ההתחברות בהישג יד.
- קישוריות רשת דו-כיוונית בין השרת ל-HA.

---

## התמונה הגדולה — סדר הפעולות

שני צדדים שצריכים "להיפגש" בשלושה ערכים: הטוקן (HA→שרת), סוד ההתראות
(שרת→HA), וכתובות. הזרימה:

1. **בונים** את השרת (`install.sh`) — מדפיס את סוד ההתראות בסוף.
2. **יוצרים** את הרשומה ב-HA ומעתיקים ממנה את הטוקן → לשרת (`pbx haivr set`).
3. **מוסיפים** טראנק ספק בשרת (`pbx trunk add`).
4. **מדביקים** את סוד ההתראות מהשרת → ל-HA, וממלאים את שדות ההתראות.
5. **בונים** את התפריט והעוזר ב-HA (יעד `ha-assist`).

השלבים למטה בפירוט.

---

## שלב 1 · התקנה

השיגו את הקבצים מ-GitHub והריצו את המתקין עם כתובת ה-HA שלכם:

```bash
git clone https://github.com/meni123/asterisk-ha-pbx.git
cd asterisk-ha-pbx
sudo bash install.sh --ha 192.168.1.10
```

> אין git? בעמוד המאגר: **Code → Download ZIP**, חלצו, והיכנסו לתיקייה.

- הדגל `--ha` **חובה** — ממנו נגזרים `local_net` וכתובת ה-AudioSocket.
  אין שום כתובת צרובה בקוד.
- ההתקנה בונה את Asterisk 22 מהמקור, מתקינה את סקריפטי ה-AGI, שירות
  ההתראות, והדיאלפלן. הפלט המלא ב-`/var/log/pbx-install.log`.
- בסוף רץ שער אימות (`verify`) — הוא נכשל בקול אם משהו חסר.

**בסיום, המתקין מדפיס את סוד ההתראות** (`PBX_ALERT_SECRET`) — סוד
אקראי שנוצר על השרת. שמרו אותו, תדביקו אותו ב-HA בשלב 4. אפשר לשלוף
אותו שוב בכל רגע:

```bash
sudo ./pbx secret
```

---

## שלב 2 · ה-front-end `pbx`

כל התפעול דרך פקודה אחת. תפריט אינטראקטיבי:

```bash
sudo ./pbx
```

או ישירות:

| פקודה | פעולה |
|---|---|
| `sudo ./pbx haivr set` | הגדרת החיבור ל-HA (כתובת + טוקן) |
| `sudo ./pbx haivr test [מספר]` | בדיקת החיבור מול ha_ivr |
| `sudo ./pbx trunk add\|list\|remove` | ניהול טראנק הספק |
| `sudo ./pbx secret` | הצגת סוד ההתראות |
| `sudo ./pbx status` | תמונת מצב המערכת |
| `sudo ./pbx install` | הרצת ההתקנה (שואל את כתובת ה-HA) |

---

## שלב 3 · חיבור ל-Home Assistant

1. ב-HA: הגדרות → הרשומה **מרכזייה עצמית** → הגדרות. המסך הראשון מציג
   את **כתובת נקודת הקצה עם הטוקן**:
   ```
   http://192.168.1.10:8123/api/ha_ivr/pbx/<TOKEN>
   ```
2. בשרת:
   ```bash
   sudo ./pbx haivr set
   ```
   הדביקו את הכתובת המלאה (הסקריפט חולץ ממנה את הטוקן לבד), ואשרו
   בדיקה. תוצאה תקינה: `HTTP 200`, `menu: True`.

> אם ה-`test` מחזיר "the number is not recognised" — זה **תקין** אם
> התפריט מסנן לפי מספר. לבדיקת המסלול האמיתי: `sudo ./pbx haivr test <המספר-שלך>`.

---

## שלב 4 · טראנק הספק

```bash
sudo ./pbx trunk add
```

האשף שואל: שם טראנק, host של הספק, פורט, האם נרשם (register), משתמש,
סיסמה, והקשר כניסה — **בחרו 1 (`from-external`)**, זה המסלול של ha_ivr
(תפריט + עוזר). בסיום מתבצע `pjsip reload` ומוצגים הרישומים.

> **שים לב:** השתמשו בטראנק הייעודי למרכזייה הזאת. ספק SIP מנתב
> שיחות נכנסות למי שנרשם אחרון — אם אותם פרטים כבר רשומים במרכזייה
> אחרת פעילה, השיחות שלה יעברו לכאן.

---

## שלב 5 · התראות קוליות

**השרת שמאזין להתראות** (`call_trigger`, על `BOX_IP:5000`) כבר הותקן
והופעל אוטומטית ע"י `install.sh` — אין מה להפעיל ידנית. הוא מקבל את
ה-POST מישות ה-`notify` של HA, מחייג ומקריא. נותר רק להצמיד את הסוד
ולמלא את שדות ההתראה.

בצד HA, ברשומה **מרכזייה עצמית** → הגדרות התפריט, מלאו:

| שדה | ערך |
|---|---|
| כתובת שירות ההתראות | `http://<כתובת-השרת>:5000/ha` |
| טראנק יוצא להתראות | שם הטראנק משלב 4 (למשל `mytrunk`) |
| סוד שירות ההתראות | הערך מ-`sudo ./pbx secret` — **חייב להיות זהה** |

הסוד חייב להיות זהה בית-בבית לזה שבשרת, אחרת כל בקשה חוזרת `403`.
לאחר מכן הוסיפו **נמענים** מכפתור ה-`+` בכרטיס — כל נמען הופך לישות
`notify` שמחייגת למספרו.

---

## שלב 6 · התפריט והעוזר הקולי ב-HA

הכל נבנה מכפתור ה-`+` בכרטיס האינטגרציה:

- **תפריט ופריטים** — תת-תפריטים ופעולות.
- **העוזר הקולי** — הוסיפו פריט "מעבר" (goto), וקבעו את היעד שלו
  בדיוק למחרוזת **`ha-assist`**. זה מה שהדיאלפלן מזהה כדי להזרים את
  השיחה ל-AudioSocket.
- **כתובת/פורט האזנה** (הגדרות התפריט): ברירת מחדל `0.0.0.0` / `9010`.
  אם HA ו-Asterisk על אותה קופסה — `127.0.0.1`. הפורט חייב להתאים
  ל-`HA_IVR_AUDIOSOCKET` בדיאלפלן (ברירת מחדל 9010).

---

## שלב 7 · אימות

```bash
sudo ./pbx status
```

ואז שיחת בדיקה: חייגו → שמעו את התפריט → הקישו את מקש העוזר → שוחחו.
שלחו התראה מ-HA → הטלפון מצלצל ומקריא.

ביומן ה-HA: `AudioSocket: line ... for <מתקשר>` (עוזר) ו-
`PBX alert -> HTTP 200` (התראה). בשרת: `AudioSocket(...:9010)`.

---

## נתיבים — לעיון

| רכיב | נתיב |
|---|---|
| סקריפטי AGI | `/var/lib/asterisk/agi-bin/` (`ha_relay.py`, `edge_say.sh`) |
| הגדרת החיבור ל-HA | `/var/lib/asterisk/agi-bin/config.ini` |
| דיאלפלן | `/etc/asterisk/extensions.conf` (+ `extensions_alerts.conf`) |
| טראנקים | `/etc/asterisk/pjsip_trunks.conf` |
| שירות ההתראות | `/usr/local/lib/pbx-alert/call_trigger.py` |
| סוד ההתראות | `/etc/pbx-alert.env` (מצב 600) |
| יומן התקנה | `/var/log/pbx-install.log` |

---

## למה הטרמינל באנגלית

ממשק עברי בטרמינל אינו מעשי: טרמינלים חלוקים על אלגוריתם ה-bidi של
יוניקוד, ושתי המשפחות דורשות סדר בתים **הפוך** (לוגי מול ויזואלי) כדי
להציג עברית נכון — אין רצף בתים שנכון בשתיהן. ממשק עברי שייך לדפדפן
(`dir="rtl"` אוניברסלי), ושם הוא נמצא: מסך ההגדרות של ha_ivr.
